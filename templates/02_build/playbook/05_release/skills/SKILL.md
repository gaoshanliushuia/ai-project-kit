---
name: release-management
description: 支持 `版本发布与迭代` 阶段的通用执行、检查和交付整理。适用于需要按 ai-project-kit 阶段标准推进 `版本发布与迭代`、生成阶段产物初稿、检查交接材料或沉淀复用经验时。
---

# 版本发布与迭代 Skill

## 目标

把通过验证的代码、配置、脚本和说明组织成可发布、可回滚、可复盘的版本。

## 适用角色

- `Release Manager Agent`

## 输入

发布范围、版本号、构建产物、配置项、测试结论、风险接受、上线窗口。

## 输出

发布计划、发布清单、部署说明、回滚说明、发布验证记录、版本复盘。

## 执行步骤

- 确认版本范围、包含项、排除项和已知问题。
- 准备构建产物、脚本、配置、部署步骤和回滚步骤。
- 完成环境、权限、依赖、容量和备份检查。
- 记录发布验证结果、异常处理和关闭状态。
- 汇总发布问题和下一轮迭代建议。

## 使用要求

- 先读取本阶段 `rules/RULE.md`，再生成或检查阶段产物。
- 所有结论必须能追踪到输入材料、编号、负责人或评审记录。
- AI 生成内容只作为初稿或检查建议，关键结论必须由阶段负责人确认。
- 发现可复用经验时，回写本阶段 `rules/`、`skills/` 或 `prompts/`，并在后续版本发布中说明。
