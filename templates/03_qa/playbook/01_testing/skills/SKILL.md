---
name: quality-assurance
description: 支持 `测试与质量保证` 阶段的通用执行、检查和交付整理。适用于需要按本项目阶段标准推进 `测试与质量保证`、生成阶段产物初稿、检查交接材料或沉淀复用经验时。
---

# 测试与质量保证 Skill

## 目标

验证需求、设计、代码和发布物是否满足预期，并形成可追踪的质量证据。

## 适用角色

- `Tester Agent`

## 输入

验收条件、设计流程、接口契约、数据规则、测试版本、环境信息、历史缺陷。

## 输出

测试策略、测试用例、执行记录、缺陷清单、复测记录、质量报告和发布建议。

## 标准执行顺序

1. 激活本阶段 `skills/SKILL.md` 作为执行入口，确认目标、输入输出和执行边界。
2. 再读取本阶段 `rules/RULE.md`，检查进入条件、覆盖要求、缺陷记录要求和退出要求。
3. 再读取本阶段 `prompts/PROMPT.md`，按标准输出结构组织测试方案、执行记录和质量结论。
4. 若输入材料不完整，先列出缺口、假设、待确认事项和影响范围，不得直接编造测试范围、结论或缺陷关闭状态。
5. 生成或检查阶段产物时，必须引用需求编号、设计编号、变更编号、版本、环境和评审记录。
6. 输出前必须按 `rules/RULE.md` 与 `prompts/PROMPT.md` 的“使用后检查”完成自检。
7. 若发现本阶段 playbook 存在冲突、过时或缺漏，先在结果中指出，再回写修正 `rules/`、`skills/` 或 `prompts/`。

## 执行步骤

- 明确测试范围、类型、优先级、风险和退出标准。
- 设计功能、接口、数据、权限、性能、安全和回归用例。
- 准备测试数据、环境、版本和配置记录。
- 登记缺陷、复测结果、影响范围和证据。
- 输出质量报告和是否建议发布的结论。

## 使用要求

- 本阶段执行入口默认是：激活 `skills/SKILL.md`，再遵循 `rules/RULE.md` 与 `prompts/PROMPT.md`。
- 生成阶段产物前，必须先确认输入完整性、编号前缀、引用版本、人工确认点和下游交接对象。
- 所有结论必须能追踪到输入材料、编号、负责人或评审记录。
- AI 生成内容只作为初稿或检查建议，关键结论必须由阶段负责人确认。
- 输出前必须完成一次 playbook 自检，至少覆盖进入条件、编号追踪、产物结构、AI 治理留痕和交接要求。
- 发现可复用经验时，回写本阶段 `rules/`、`skills/` 或 `prompts/`，并在后续版本发布中说明。
