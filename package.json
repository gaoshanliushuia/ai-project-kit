{
  "name": "ai-project-kit",
  "version": "0.1.0",
  "description": "AI-assisted software project framework initializer.",
  "type": "commonjs",
  "files": [
    "ai-project-kit.js",
    "templates",
    "scripts",
    "README.md"
  ],
  "scripts": {
    "init": "node ai-project-kit.js",
    "validate:sync-config": "node ai-project-kit.js .tmp/sync-validate all && node scripts/cursor-script/sync-cursor.mjs --root .tmp/sync-validate --only rules --dry-run && node scripts/cursor-script/sync-cursor.mjs --root .tmp/sync-validate --only skills --dry-run"
  },
  "keywords": [
    "ai",
    "project-template",
    "software-engineering",
    "workflow",
    "initializer"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
