---
name: architecture-design
description: 支持 `系统架构设计` 阶段的通用执行、检查和交付整理。适用于需要按本项目阶段标准推进 `系统架构设计`、生成阶段产物初稿、检查交接材料或沉淀复用经验时。
---

# 系统架构设计 Skill

## 目标

把需求基线转化为系统边界、架构方案、技术决策和质量属性设计。

## 适用角色

- `Architect Agent`

## 输入

需求基线、核心流程、非功能目标、外部系统、部署环境、技术约束。

## 输出

系统上下文、模块边界、架构决策、集成边界、非功能方案、架构风险。

## 标准执行顺序

1. 先读取本阶段 `skills/SKILL.md`，确认目标、输入输出和执行边界。
2. 再读取本阶段 `rules/RULE.md`，检查进入条件、编号规则、质量属性要求和退出要求。
3. 再读取本阶段 `prompts/PROMPT.md`，按标准输出结构组织架构设计产物。
4. 若输入材料不完整，先列出缺口、假设、待确认事项和影响范围，不得直接编造技术或环境事实。
5. 生成或检查阶段产物时，必须引用需求编号、非功能目标、外部约束、版本和评审记录。
6. 输出前必须按 `rules/RULE.md` 与 `prompts/PROMPT.md` 的“使用后检查”完成自检。
7. 若发现本阶段 playbook 存在冲突、过时或缺漏，先在结果中指出，再回写修正 `rules/`、`skills/` 或 `prompts/`。

## 执行步骤

- 提取架构驱动因素和质量属性优先级。
- 定义系统边界、上下游依赖、主要数据流和用户入口。
- 比较候选架构方案，记录取舍理由和放弃原因。
- 补齐安全、容量、日志、监控、容灾和运维设计。
- 形成架构风险清单和验证计划。

## 使用要求

- 本阶段执行入口默认是：`skills/SKILL.md` -> `rules/RULE.md` -> `prompts/PROMPT.md`。
- 生成阶段产物前，必须先确认输入完整性、编号前缀、引用版本、人工确认点和下游交接对象。
- 所有结论必须能追踪到输入材料、编号、负责人或评审记录。
- AI 生成内容只作为初稿或检查建议，关键结论必须由阶段负责人确认。
- 输出前必须完成一次 playbook 自检，至少覆盖进入条件、编号追踪、产物结构、AI 治理留痕和交接要求。
- 发现可复用经验时，回写本阶段 `rules/`、`skills/` 或 `prompts/`，并在后续版本发布中说明。
